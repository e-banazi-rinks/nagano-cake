<div class="container">
  <div class="row mt-5">
    <div class="col-12">
      <h4>ログイン</h4>
      <p>会員の方はこちらからログイン</p>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <table>
        <tbody>
          <tr>
            <div class="field">
              <td><b><%= f.label :メールアドレス %></b></td>
              <td><%= f.email_field :email, autofocus: true, autocomplete: "email", size: "40×5" %></td>
            </div>
          </tr>
          <tr>
            <div class="field">
            <td><b><%= f.label :パスワード %></b></td>
            <td><%= f.password_field :password, autocomplete: "current-password", size: "40×5" %></tdr>
            </div>
          </tr>
          <tr>
            <% if devise_mapping.rememberable? %>
            <div class="field">
              <td>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </td>
            </div>
            <% end %>
          </tr>
          <tr>
            <div class="actions">
              <td></td>
              <td><%= f.submit "ログイン", class: 'btn btn-success' %></td>
            </div>
          </tr>
        </tbody>
      </table>
      <% end %>
    </div>
  </div>
  <div class="mt-5">
    <h4>会員登録がお済みでない方</h4>
    <p><%= render "public/shared/links" %>から新規登録を行ってください。</p>
  </div>
</div>

